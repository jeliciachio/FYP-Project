<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Your Cards - HoH Bank</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@700&family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">
  <style>
    html, body {
      margin: 0;
      padding: 0;
      min-height: 100vh;
      width: 100vw;
      box-sizing: border-box;
      font-family: 'Inter', 'Georgia', serif;
      background: linear-gradient(120deg, #232946 0%, #0f172a 100%);
      color: #f3f6fa;
      overflow-x: hidden;
    }
    .navbar {
      background: #181f2f;
      box-shadow: 0 2px 16px #0002;
      padding: 0.7rem 0;
    }
    .navbar-brand {
      font-family: 'Orbitron', 'Inter', 'Georgia', serif;
      color: #7df9ff !important;
      font-size: 2rem;
      letter-spacing: 0.04em;
    }
    .nav-link, .dropdown-toggle {
      color: #e0eaff !important;
      font-weight: 600;
      margin-right: 1.2rem;
    }
    .nav-link.active, .nav-link:focus, .nav-link:hover {
      color: #22c55e !important;
    }
    .navbar-nav .nav-link {
      color: #7df9ff !important;
      font-weight: 700;
      transition: color 0.3s ease;
      text-shadow: 0 0 8px #0ff, 0 0 2px #fff;
    }
    .navbar-nav .nav-link:hover,
    .navbar-nav .nav-link.active {
      text-decoration: underline;
      color: #7df9ff !important;
    }
    .cards-header { background: #181f2f; border-radius: 18px; box-shadow: 0 8px 32px #7df9ff22; padding: 2rem 1.5rem; margin: 2rem 0; text-align: center; }
    .cards-header h1 { font-family: 'Orbitron', 'Inter', 'Georgia', serif; color: #7df9ff; font-size: 2.2rem; }
    .card { border: 2px solid #181f2f !important; border-radius: 18px !important; box-shadow: 0 4px 24px #7df9ff22; }
    .card-title { color: #7df9ff; font-family: 'Orbitron',sans-serif; font-size: 1.3rem; }
    .card-body { color: #e0eaff; }
    .card-footer { background: transparent; border: none; }
    .btn-hoh { background: linear-gradient(90deg, #22c55e 0%, #7df9ff 100%); color: #181f2f; font-weight: bold; border: none; border-radius: 8px; font-size: 1.1rem; padding: 10px 0; margin-top: 0.5rem; box-shadow: 0 2px 8px #22c55e33; }
    .btn-hoh:hover { background: linear-gradient(90deg, #7df9ff 0%, #22c55e 100%); color: #fff; }
    .btn-hoh, .btn-secondary { width: 100%; font-size: 1.05rem; border-radius: 8px; }
    .btn-secondary { background: #232946; color: #7df9ff; border: 2px solid #7df9ff; }
    .btn-secondary:hover { background: #7df9ff; color: #232946; }
    .card-label { color: #22c55e; font-weight: bold; }
    .card-value { color: #e0eaff; }
    .card-status { font-weight: bold; }
    .card-status .bi { font-size: 1.1rem; vertical-align: middle; }
    @media (max-width: 900px) {
      .row-cols-md-2 > .col, .row-cols-lg-3 > .col { flex: 0 0 100%; max-width: 100%; }
    }
    @media (max-width: 600px) {
      .cards-header { padding: 1rem 0.3rem; }
      .card { margin-bottom: 1.2rem; }
      .card-title { font-size: 1.1rem; }
      .btn-hoh, .btn-secondary { font-size: 0.98rem; }
    }
  </style>
</head>
<body>
<nav class="navbar navbar-expand-lg navbar-dark">
    <div class="container-fluid">
 <a class="navbar-brand" href="/dashboard">HoH Bank</a>      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item"><a class="nav-link" href="/my-products">My Products</a></li>
          <li class="nav-item"><a class="nav-link" href="/goals">Goals</a></li>          
          <li class="nav-item"><a class="nav-link" href="/customer/consultations">Consultations</a></li>
          <li class="nav-item"><a class="nav-link" href="/user-transactions">Transactions</a></li>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="financialLearningDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              Financial Learning Tool
            </a>
            <ul class="dropdown-menu" aria-labelledby="financialLearningDropdown">
              <li><a class="dropdown-item" href="/financial-education">📘 Student Financial Education</a></li>
              <li><a class="dropdown-item" href="/financial-resources">📚 Financial Resources</a></li>
              <li><a class="dropdown-item" href="/financial-quiz">🧠 Financial Quiz</a></li>
            </ul>
          </li>
          <li class="nav-item"><a class="nav-link" href="/profile-edit">Profile</a></li>
          <li class="nav-item"><a class="nav-link" href="/logout/customer">Logout</a></li>
        </ul>
      </div>
    </div>
  </nav>
  <div class="container">
    <div class="cards-header">
      <span class="rounded-circle shadow-lg d-inline-block mb-2" style="background:#181f2f;padding:18px;">
        <i class="bi bi-credit-card" style="font-size:2.7rem;color:#7df9ff;"></i>
      </span>
      <h1 style="font-size:2.2rem;">Your Cards</h1>
      <p style="color:#b6c2e2;font-size:1.1rem;">See all your debit and credit cards in one place.</p>
    </div>
    <% if (cards && cards.length > 0) { %>
      <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-4 justify-content-center">
        <% cards.forEach(function(card) { %>
          <div class="col d-flex align-items-stretch">
            <div class="card h-100 shadow-lg">
              <div class="card-body">
                <div class="d-flex align-items-center mb-3">
                  <span class="rounded-circle d-inline-block me-3" style="background:#181f2f;padding:12px;">
                    <i class="bi bi-credit-card" style="font-size:1.7rem;color:#7df9ff;"></i>
                  </span>
                  <h5 class="card-title mb-0">**** **** **** <%= card.card_number.slice(-4) %></h5>
                </div>
                <div class="mb-2"><span class="card-label">Type:</span> <span class="card-value"><%= card.card_type ? card.card_type : (card.product_id ? 'Linked' : '-') %></span></div>
                <div class="mb-2"><span class="card-label">Balance:</span> <span class="card-value">$<%= typeof card.balance === 'number' ? card.balance.toFixed(2) : Number(card.balance || 0).toFixed(2) %></span></div>
                <div class="mb-2 card-status">
                  <span class="card-label">Status:</span> <span class="card-value"><%= card.card_status %></span>
                  <% if (card.card_status === 'active') { %>
                    <i class="bi bi-check-circle-fill text-success ms-1"></i>
                  <% } else { %>
                    <i class="bi bi-x-circle-fill text-danger ms-1"></i>
                  <% } %>
                </div>
                <div class="mb-2"><span class="card-label">Expiry:</span> <span class="card-value"><%= card.expiry_date ? card.expiry_date.toISOString().slice(0,10) : '-' %></span></div>
                <div class="mb-2"><span class="card-label">Issued At:</span> <span class="card-value"><%= card.issued_at ? card.issued_at.toISOString().slice(0,10) : '-' %></span></div>
              </div>
              <div class="card-footer d-flex justify-content-center">
                <button class="btn btn-hoh" disabled>View Details</button>
              </div>
            </div>
          </div>
        <% }); %>
      </div>
    <% } else { %>
      <div class="text-center py-5">
        <i class="bi bi-credit-card text-secondary" style="font-size:3rem;"></i>
        <p class="mt-3" style="color:#b6c2e2;font-size:1.2rem;">No cards found.</p>
      </div>
    <% } %>
    <div class="text-center mt-4">
      <a href="/dashboard" class="btn btn-secondary w-100" style="max-width:400px;margin:auto;"><i class="bi bi-arrow-left"></i> Back to Dashboard</a>
    </div>
  </div>
</body>
</html>
