<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Consultation Booking | HoH Bank</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet"/>
  <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@700&family=Inter:wght@400;600;700&display=swap" rel="stylesheet"/>
  <style>
    html, body {
      margin: 0;
      padding: 0;
      min-height: 100vh;
      width: 100vw;
      box-sizing: border-box;
      font-family: 'Inter', sans-serif;
      background: radial-gradient(ellipse at 60% 10%, #232946 0%, #0f172a 100%);
      color: #f3f6fa;
      overflow-x: hidden;
    }

    .stars {
      position: fixed;
      width: 100vw;
      height: 100vh;
      top: 0;
      left: 0;
      z-index: 0;
      pointer-events: none;
    }

    .star {
      position: absolute;
      background: white;
      border-radius: 50%;
      opacity: 0.7;
      animation: twinkle 2s infinite alternate;
      box-shadow: 0 0 8px #7df9ff;
    }

    @keyframes twinkle {
      0% { opacity: 0.5; }
      100% { opacity: 1; }
    }
   .navbar {
      position: fixed;
      top: 0;
      width: 100vw;
      background: linear-gradient(135deg, #232946 0%, #1e2a3a 100%);
      backdrop-filter: blur(10px);
      box-shadow: 0 4px 20px rgba(0, 0, 0, 0.3);
      z-index: 10;
      padding: 1rem 0;
      border-bottom: 1px solid rgba(125, 249, 255, 0.2);
    }

    .navbar-brand {
      font-weight: 800;
      font-size: 1.6rem;
      color: #7df9ff !important;
      text-shadow: 0 0 15px rgba(125, 249, 255, 0.6);
      letter-spacing: 1.2px;
      font-family: 'Orbitron', sans-serif;
    }

    .navbar-nav .nav-link {
      color: #cbd5e1 !important;
      font-weight: 500;
      transition: all 0.3s ease;
      padding: 0.5rem 1rem !important;
      border-radius: 8px;
      margin: 0 0.25rem;
    }

    .navbar-nav .nav-link:hover {
      color: #7df9ff !important;
      background: rgba(125, 249, 255, 0.1);
      transform: translateY(-2px);
    }

    .navbar-nav .nav-link.active {
      color: #22c55e !important;
      background: rgba(34, 197, 94, 0.1);
    }

    .navbar-toggler {
      border-color: rgba(125, 249, 255, 0.3);
    }

    .navbar-toggler-icon {
      filter: brightness(0) invert(1) drop-shadow(0 0 2px #7df9ff);
    }    .container {
      max-width: 1000px;
      margin: 0 auto;
      padding: 40px;
      background: linear-gradient(135deg, #232946 0%, #1e2a3a 100%);
      border-radius: 24px;
      box-shadow: 
        0 25px 60px rgba(34, 197, 94, 0.15),
        0 10px 30px rgba(125, 249, 255, 0.1),
        inset 0 1px 0 rgba(255, 255, 255, 0.1);
      border: 1px solid rgba(125, 249, 255, 0.2);
      position: relative;
      overflow: hidden;
      animation: fadeInUp 0.6s ease-out;
    }


    .btn-hoh {
      background: linear-gradient(90deg, #22c55e, #7df9ff);
      color: #181f2f;
      font-weight: 700;
      border: none;
      border-radius: 8px;
      box-shadow: 0 2px 8px #22c55e33;
      transition: 0.2s;
    }

    .btn-hoh:hover {
      transform: translateY(-2px) scale(1.03);
      box-shadow: 0 8px 24px #22c55e44;
      color: #fff;
    }

    .table thead {
      background-color: #22c55e;
      color: #000;
    }

    .neon-heading {
  font-family: 'Orbitron', sans-serif;
  font-size: 2.2rem;
  text-shadow: 
    0 0 5px #7df9ff, 
    0 0 10px #7df9ff, 
    0 0 20px #22c55e, 
    0 0 40px #22c55e;
  color: #7df9ff;
  animation: glowFlicker 1.5s infinite alternate;
}

@keyframes glowFlicker {
  from {
    text-shadow: 
      0 0 4px #7df9ff, 
      0 0 8px #7df9ff, 
      0 0 15px #22c55e;
  }
  to {
    text-shadow: 
      0 0 8px #7df9ff, 
      0 0 12px #7df9ff, 
      0 0 25px #22c55e;
  }
}


    .glass-card {
      background: rgba(255, 255, 255, 0.05);
      border-radius: 12px;
      box-shadow: 0 4px 20px rgba(0, 255, 255, 0.1);
      padding: 30px;
      backdrop-filter: blur(10px);
      border: 1px solid rgba(125, 249, 255, 0.1);
    }
  </style>
</head>

<body>
<nav class="navbar navbar-expand-lg navbar-dark">
    <div class="container-fluid">
 <a class="navbar-brand" href="/dashboard">HoH Bank</a>      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item"><a class="nav-link" href="/my-products">My Products</a></li>
          <li class="nav-item"><a class="nav-link" href="/goals">Goals</a></li>          
          <li class="nav-item"><a class="nav-link" href="/customer/consultations">Consultations</a></li>
          <li class="nav-item"><a class="nav-link" href="/user-transactions">Transactions</a></li>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="financialLearningDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              Financial Learning Tool
            </a>
            <ul class="dropdown-menu" aria-labelledby="financialLearningDropdown">
              <li><a class="dropdown-item" href="/financial-education">📘 Student Financial Education</a></li>
              <li><a class="dropdown-item" href="/financial-resources">📚 Financial Resources</a></li>
              <li><a class="dropdown-item" href="/financial-quiz">🧠 Financial Quiz</a></li>
            </ul>
          </li>
          <li class="nav-item"><a class="nav-link" href="/profile-edit">Profile</a></li>
          <li class="nav-item"><a class="nav-link" href="/logout/customer">Logout</a></li>
        </ul>
      </div>
    </div>
  </nav>
<div class="stars"></div>

<div class="container mb-4" style="margin-top: 80px;">
  <div class="row g-4">
    <div class="col-md-4">
      <div class="card text-center bg-dark border-success shadow" style="border-radius: 16px;">
        <div class="card-body">
          <h5 class="text-success">Upcoming</h5>
          <p class="fs-3 fw-bold text-light" style="text-shadow: 0 0 10px #7df9ff;"><%= upcomingCount %></p>
        </div>
      </div>
    </div>
    <div class="col-md-4">
      <div class="card text-center bg-dark border-info shadow" style="border-radius: 16px;">
        <div class="card-body">
          <h5 class="text-info">Completed</h5>
          <p class="fs-3 fw-bold text-light" style="text-shadow: 0 0 10px #7df9ff;"><%= completedCount %></p>
        </div>
      </div>
    </div>
    <div class="col-md-4 text-center">
      <a href="/customer/history" class="btn btn-hoh mt-md-4 w-100">View Consultation History</a>
    </div>
  </div>
</div>


<div class="container py-5">
  <div class="glass-card">
    <h2 class="mb-5 text-center neon-heading">🚀 Book a Financial Consultation</h2>


    <table class="table table-bordered table-hover text-light">
      <thead class="text-dark">
        <tr>
          <th>Advisor Name</th>
          <th>Appointment Date</th>
          <th>Status</th>
          <th>Action</th>
        </tr>
      </thead>
      <tbody>
        <% if (slots.length === 0) { %>
          <tr>
            <td colspan="4" class="text-center">No available slots right now.</td>
          </tr>
        <% } else { %>
          <% slots.forEach(slot => { %>
            <tr>
              <td><%= slot.advisor_name || '—' %></td>
              <td><%= new Date(slot.appointment_date).toLocaleString() %></td>
              <td>
                <% 
                  const status = slot.status.toLowerCase();
                  const wasAccepted = slot.was_accepted_before;
                  const statusLabel = status === 'accepted' ? 'Accepted'
                                   : status === 'pending accept' ? 'Pending Accept'
                                   : status === 'booked' && wasAccepted ? 'Rescheduled'
                                   : status === 'booked' ? 'Pending'
                                   : status.charAt(0).toUpperCase() + status.slice(1);
                %>

                <% if (statusLabel === 'Accepted') { %>
                  <span class="badge bg-success"><%= statusLabel %></span>
                <% } else if (statusLabel === 'Pending Accept') { %>
                  <span class="badge bg-warning text-dark"><%= statusLabel %></span>
                <% } else if (statusLabel === 'Rescheduled') { %>
                  <span class="badge bg-info"><%= statusLabel %></span>
                <% } else if (statusLabel === 'Pending') { %>
                  <span class="badge bg-secondary"><%= statusLabel %></span>
                <% } else { %>
                  <span class="badge bg-light text-dark"><%= statusLabel %></span>
                <% } %>
              </td>

              <td>
                <% if (slot.status === 'available') { %>
                  <form action="/customer/book/<%= slot.consultation_id %>" method="POST">
                    <button type="submit" class="btn btn-hoh btn-sm">Book</button>
                  </form>
                <% } else if ((['booked', 'accepted', 'pending accept'].includes(slot.status)) && slot.customer_id === user_id) { %>
                  <% if (slot.status === 'pending accept') { %>
                    <form action="/customer/accept-reschedule/<%= slot.consultation_id %>" method="POST">
                      <button type="submit" class="btn btn-success btn-sm mb-1">Accept Rescheduled Slot</button>
                    </form>
                  <% } %>
                  <form action="/customer/cancel/<%= slot.consultation_id %>" method="POST">
                    <button type="submit" class="btn btn-warning btn-sm">Cancel</button>
                  </form>
                <% } else { %>
                  <span class="text-muted">Not Available</span>
                <% } %>
              </td>
            </tr>
          <% }) %>
        <% } %>
      </tbody>
    </table>
  </div>
</div>

<script>
  function createStars(num) {
    const stars = document.querySelector('.stars');
    for (let i = 0; i < num; i++) {
      const star = document.createElement('div');
      star.className = 'star';
      const size = Math.random() * 2 + 1;
      star.style.width = `${size}px`;
      star.style.height = `${size}px`;
      star.style.top = `${Math.random() * 100}vh`;
      star.style.left = `${Math.random() * 100}vw`;
      star.style.opacity = Math.random() * 0.7 + 0.3;
      star.style.animationDuration = `${1.5 + Math.random() * 2}s`;
      stars.appendChild(star);
    }
  }
  createStars(100);
</script>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

